import React, {Fragment} from 'react'
import ProjectList from './ProjectList'

const SidePanel = () => {
  return (
    <React.Fragment>
      <ProjectList />
    </React.Fragment>
  )
};

export default SidePanel;
